# Configuración del Servidor
NODE_ENV=development
PORT=3001
BACKEND_URL=http://localhost:3001

# Base de Datos PostgreSQL
DATABASE_URL="postgresql://akemy:akemy123@localhost:5432/akemy_db?schema=public"

# JWT
JWT_SECRET=akemy_super_secret_jwt_key_2024_very_secure
JWT_REFRESH_SECRET=akemy_super_secret_refresh_key_2024_very_secure
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:3000

# Configuración de Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu_correo@gmail.com
SMTP_PASS=tu_password_de_aplicacion
SMTP_FROM="AKEMY Papelería <noreply@akemy.com>"

# Uploads (Local - solo se usa si no hay Cloudinary configurado)
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# Cloudinary (para almacenamiento de imágenes en la nube)
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=100

